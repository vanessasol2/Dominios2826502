public class OperadoresTernarios {
    public static void main(String[] args) {
        int cuenta = 11;
        String mensaje = "";
        mensaje = (cuenta < 10) ? "Cuenta es mayor a 10" : "Cuenta menor a 10";
        System.out.println(mensaje);

       // resultado = (condición) ? expresión1 : expresión2;
       //condición: Es una expresión booleana que se evalúa.
       //expresión1: Es el valor que se asigna a resultado si la condición es true.
       //expresión2: Es el valor que se asigna a resultado si la condición es false

       
    }

}

package org.cgmlti.adso.ciclos;

//import java.util.Scanner;//

public class Notas {
    public static void main(String[] args) {
        // Crear un Scaner
        /*
         * Scanner lector = new Scanner(System.in);
         * // Acumulador;
         * int suma = 0;
         * int contador = 0;
         * 
         * for (int i = 0; i < 10; i++) {
         * // leer una nota
         * System.out.println("Ingrese una nota:");
         * // Capturar al entero por telado
         * int nota = lector.nextInt();
         * suma += nota;
         * System.out.println(nota);
         * 
         * if (suma >= 6) {
         * suma += contador;
         * contador++;
         * }
         * 
         * }
         * // calcular promedio
         * System.out.println("Promedio de notas es:" +
         * suma / 10);
         */
    }
}
